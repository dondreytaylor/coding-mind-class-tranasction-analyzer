# TODO List

## Part 1
We would like to build an application is capable of reading in transactions from a file, parsing them, and then giving us some insight about them. The insight should be represented as both JSON or CSV data that can be used to build a report and also formatted in a way where we can graph it using a graphing library. 

[Plan]
- Create a python file that contains a function that reads in a file from a file path
- Parses the file into a list that maps to an class 
- Exports this data into a new file


## Part 2
The application should also have a website that corresponds with it. The website will be able to read a file from your operationg system that has a list of transactions and then extract/display the data and render one or more graphs that fits the data. 

- 